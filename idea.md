User submits task
→ wallet sends funds (real)
→ task stored
→ ONE agent picks it
→ agent processes
→ artifact returned
→ payment shown

Phase 2 — Add pseudo competition

Then:

3 agents generate bids (with delay)
→ best selected
→ winner executes


agents = [
  { name: "Builder", latency: 5s },
  { name: "Designer", latency: 8s },
  { name: "Writer", latency: 6s }
]

also 
agents = [
  { name: "Builder", latency: 5s },
  { name: "Designer", latency: 8s },
  { name: "Writer", latency: 6s }
]